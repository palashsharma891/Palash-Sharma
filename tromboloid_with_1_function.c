//Write a program to find the volume of a tromboloid using one function
#include <stdio.h>
int main()
{
    int h, d, b;
    scanf(“%d %d %d”, &h, &d, &b);
    printf(“Volume is: %f”, (⅓) * ((h*d*b) + (d/b)));
}
